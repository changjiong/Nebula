# ===========================================
# Nebula 星图 - 环境变量配置模板
# ===========================================
# 复制此文件为 .env 并填写实际值
# cp .env.example .env
# ===========================================

# ===== 域名配置 =====
# 本地开发使用 localhost，生产环境使用实际域名
DOMAIN=localhost
# 测试 Traefik 子域名路由时使用
# DOMAIN=localhost.tiangolo.com

# 前端地址（用于后端生成邮件链接）
FRONTEND_HOST=http://localhost:5173
# 生产环境示例: FRONTEND_HOST=https://dashboard.example.com

# 环境标识: local, staging, production
ENVIRONMENT=local

# ===== 项目配置 =====
PROJECT_NAME="Nebula 星图"
STACK_NAME=nebula
CI=false

# ===== 后端配置 =====
# CORS 允许的来源（逗号分隔）
BACKEND_CORS_ORIGINS="http://localhost,http://localhost:5173,https://localhost,https://localhost:5173"

# 安全密钥（重要！生产环境必须更改）
# 生成命令: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=changethis

# 初始超级管理员
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=changethis

# ===== 邮件配置 =====
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_TLS=True
SMTP_SSL=False
EMAILS_FROM_EMAIL=noreply@example.com

# ===== 数据库配置 =====
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=postgres
# 生产环境必须更改此密码
POSTGRES_PASSWORD=changethis

# ===== 监控配置 =====
SENTRY_DSN=

# ===== Docker 镜像配置 =====
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend

# ===== AI 模型配置 =====
# DeepSeek API (https://platform.deepseek.com/)
DEEPSEEK_API_KEY=sk-your-api-key-here
DEEPSEEK_API_BASE=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat

# 其他兼容 OpenAI API 的提供商示例:
# OpenAI: DEEPSEEK_API_BASE=https://api.openai.com/v1, LLM_MODEL=gpt-4
# SiliconFlow: DEEPSEEK_API_BASE=https://api.siliconflow.cn/v1, LLM_MODEL=deepseek-ai/DeepSeek-V3
